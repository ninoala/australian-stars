{
  "name": "australian-stars-theme",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sass": "sass --watch sass/style.scss:style.css --style=expanded --no-source-map",
    "build": "sass sass/style.scss style.css --style=compressed --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.77.8"
  },
  "dependencies": {
    "@fontsource/poppins": "^5.3.0"
  }
}